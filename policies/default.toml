schema = "ward.policy.v1"

# Ward's non-removable baseline is embedded in the Go policy package. This file
# demonstrates the only supported extension shape: additive deny rules. There
# are intentionally no allow, exception, mode, or replacement fields.
[deny]
paths = []
commands = []
